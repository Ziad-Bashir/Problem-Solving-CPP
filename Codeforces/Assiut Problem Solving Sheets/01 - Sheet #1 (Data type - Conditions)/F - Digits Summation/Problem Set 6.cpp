#include <iostream>
#include<iomanip>
#include<vector>

using namespace std;

int main()
{

    vector<int> result;
    int n, m;
    int sum = 0;
    cin >> n >> m;
    result.push_back(n);
    result.push_back(m);


    cout << sum << "\n";
    
    return 0;
}